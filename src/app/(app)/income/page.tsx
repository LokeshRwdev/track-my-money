import { IncomeView } from '@/components/income/income-view'

export const metadata = {
  title: 'Cash In | Track My Money',
  description: 'Manage all money received and expected incoming streams',
}

export default function IncomePage() {
  return <IncomeView />
}
